{
  "id": "youtube_comment",
  "capability": "youtube_comment",
  "version": "1.0.0",
  "workType": "ui",
  "reloadAfterSuccess": true,
  "auth": "cookie-login-required",
  "status": "implemented-dom-unverified",
  "ok": false,
  "steps": [
    {
      "label": "open watch page",
      "workType": "ui",
      "selector": "https://www.youtube.com/watch?v={videoId}",
      "note": "one real navigation; the site's own JS renders the comment composer"
    },
    {
      "label": "focus composer + type",
      "workType": "ui",
      "selector": "ytd-comment-simplebox-renderer #placeholder-area, ytd-comment-simplebox-renderer [contenteditable=true]",
      "note": "real key events into the site's own contenteditable"
    },
    {
      "label": "press the site's Comment button",
      "workType": "ui",
      "selector": "ytd-comment-simplebox-renderer #submit-button",
      "note": "the site's own JS submits; never a synthetic fetch"
    },
    {
      "label": "read-back posted text",
      "workType": "ui",
      "selector": "#content-text, ytd-comment-renderer #content-text",
      "note": "verify the site's comment list shows the text after the real submit; ok=false until a logged-in session round-trip proves it. NEVER claims posted without this read-back."
    }
  ]
}